export interface ResuxViteBuilderOptions {
  root: string;
  outDir: string;
  dev: boolean;
}

export function createViteBuilderOptions(options: ResuxViteBuilderOptions): ResuxViteBuilderOptions {
  return options;
}
